// Frontend component for logout button
import React from 'react';

function LogoutButton() {
  return (
    <a href="/logout" className="btn btn-secondary">
      Sign Out
    </a>
  );
}

export default LogoutButton;